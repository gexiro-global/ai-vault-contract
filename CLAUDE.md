# CLAUDE.md

The operating contract for this vault lives in `@AGENTS.md`. Read it in full at the start of every session, along with `00-index/tag-registry.md` and `00-index/frontmatter-schema.md`.

Do not duplicate the rules here — this file exists only so that tools which look for `CLAUDE.md` are pointed at the single source of truth.

@AGENTS.md
